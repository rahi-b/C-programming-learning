#include <stdio.h>
void main(){
    int limit,i,ar[100],sum=0;
    printf("Enter limit of array:");
    scanf("%d",&limit);

    printf("Enter array values:\n");
    for(i=0;i<limit;i++){
        scanf("%d",&ar[i]);
    }{for(i=0;i<limit;i++)
        sum=sum+ar[i];
    }
    printf("sum of values is: \n");
        printf("%d",sum);

    }



